#include "monty.h"
/**
 * nop_fun - does nothing
 * @top: a pointer to the top of the stack
 * @count: line number
 *
 * Return: NOthing!
 */
void nop_fun(stack **top, unsigned int count)
{
	(void) count;
	(void) *top;
}
