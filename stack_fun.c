#include "monty.h"
/**
 * stack_fun - sets the format of the data to a stack (LIFO)
 * @top: a pointer to the top of the stack
 * @count: line number
 *
 * Return: Nothing!
 */
void stack_fun(stack_t **top, unsigned int count)
{
	(void)top;
	(void)count;
	jos.lifi = 0;
}
